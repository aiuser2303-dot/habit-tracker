# ===========================================
# HABIT TRACKER - ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env.local for local development
# Add these to Vercel Environment Variables for production

# ===========================================
# SUPABASE CONFIGURATION (Required)
# ===========================================
# Get these from: Supabase Dashboard > Settings > API

# Public URL (safe for client-side)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your-anon-key

# Service Role Key (server-side only - NEVER expose to client!)
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your-service-role-key

# ===========================================
# GOOGLE OAUTH (Required for Google Sign-In)
# ===========================================
# Set up in: Google Cloud Console > APIs & Services > Credentials
# Also configure in: Supabase Dashboard > Authentication > Providers > Google

VITE_GOOGLE_CLIENT_ID=123456789-abcdefg.apps.googleusercontent.com

# ===========================================
# EMAIL CONFIGURATION (Choose ONE option)
# ===========================================

# ----- OPTION 1: SendGrid (Recommended) -----
# Get API key from: SendGrid Dashboard > Settings > API Keys
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ----- OPTION 2: Gmail SMTP -----
# Enable 2FA on Gmail, then create App Password:
# Google Account > Security > 2-Step Verification > App passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password

# ----- OPTION 3: Other SMTP Providers -----
# Mailgun: smtp.mailgun.org:587
# Amazon SES: email-smtp.us-east-1.amazonaws.com:587
# Outlook: smtp-mail.outlook.com:587

# Email sender (shown in "From" field)
EMAIL_FROM=Habit Tracker <noreply@yourdomain.com>

# ===========================================
# APP CONFIGURATION
# ===========================================

# Your deployed app URL (for email links)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# Default reminder settings
DEFAULT_REMINDER_TIME=22:00
DEFAULT_REMINDER_THRESHOLD=70

# ===========================================
# SECURITY
# ===========================================

# Cron job secret (generate a random string)
# Used to secure the /api/cron/send-reminders endpoint
CRON_SECRET=generate-a-random-32-char-string-here

# ===========================================
# VERCEL ENVIRONMENT VARIABLES SETUP
# ===========================================
# In Vercel Dashboard > Your Project > Settings > Environment Variables
# Add each variable above with its value
#
# For secrets (API keys, passwords), use Vercel's encrypted secrets:
# 1. Go to Settings > Environment Variables
# 2. Add variable name (e.g., SUPABASE_SERVICE_KEY)
# 3. Add value
# 4. Select environments (Production, Preview, Development)
# 5. Click Save
